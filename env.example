# Vault RPG TOTP Configuration
# Copy this file to .env and set your actual values

# Required: TOTP secret key (Base32 encoded)
# You can generate one using: cargo run totp generate
VAULT_TOTP_SECRET=KRSXG5CTMVRXEZLUKN2XAZLSKNSWG4TFOQ

# Optional: Account name (default: default@example.com)
VAULT_TOTP_ACCOUNT=default@example.com

# Optional: Issuer name (default: not set)
VAULT_TOTP_ISSUER=Github
